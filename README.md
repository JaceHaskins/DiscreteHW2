# DiscreteHW2

To run this program i compile the code by typing:
clang++ -std=c++11 Cpp_Template-1.cpp -o relation tool.

Then to execute the program I typed:
./relation_tool

The program did successfully run, o calculate the cardinality of the transitive closure |R|, I implemented Warshall’s Algorithm, which uses a triple-nested loop structure to efficiently determine reachability across the n x n adjacency matrix.
